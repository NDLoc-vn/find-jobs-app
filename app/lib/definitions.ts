export type Job = {
  title: string;
  company: string;
  salary: string;
  location: string;
};

export type Company = {
  name: string;
  email: string;
  industry: string;
  city: string;
  address: string;
  website: string;
}